#include "regex_parser.h"
#include <stdio.h>

int main(int argc, char const *argv[])
{
    // TEST PROGRAM DELETE ONCE DONE !!!!
    // TEST PROGRAM DELETE ONCE DONE !!!!
    printf("%u\n", is_regex_match_in_line(argv[1], argv[2]));
    return 0;
}
